

.env.example
Code 
I've created a .env.example file from your environment variables! This file:

Features:

Removes sensitive data - All actual values are replaced with placeholder text
Includes helpful comments - Explains what each variable does
Provides examples - Shows the format for MongoDB URI and API keys
Security notes - Important reminders about environment variable security
Clear structure - Organized by category (API, Server, Database, etc.)
Usage Instructions:

Save this as .env.example in your project root
Create a copy named .env for actual use:
bash
cp .env.example .env
Fill in your real values in the .env file
Add .env to your .gitignore file to keep it secure:
bash
echo ".env" >> .gitignore
Security Benefits:

Developers can see what environment variables are needed
No sensitive information is exposed in version control
Clear documentation of required configuration
Helps prevent accidental commits of secrets
This .env.example file is a best practice for any project that uses environment variables!








# QuestionAI Environment Variables
# Copy this file to .env and fill in your actual values

# Gemini AI API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGODB_URI=your_mongodb_connection_string_here

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# File Upload Settings
MAX_FILE_SIZE=52428800
ALLOWED_FILE_TYPES=pdf,jpg,jpeg,png

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001